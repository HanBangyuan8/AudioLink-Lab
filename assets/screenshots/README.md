# Screenshot assets

`new-measurement-workflow.png` is reserved for the first public screenshot of
the complete Reference → Recording → Configure → Analyze → Review workflow.
The README intentionally uses a textual placeholder until release packaging and
visual QA are complete, so a missing image is not rendered as a broken asset.
